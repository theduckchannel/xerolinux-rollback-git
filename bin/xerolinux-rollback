#!/usr/bin/python
import xerolinux-rollback.mainwindow
import sys
import os

if __name__ == '__main__':
    os.environ['QT_LOGGING_RULES'] = "qt5ct.debug=false"
    window = xerolinux-rollback.mainwindow.MainWindow()
    app = window.app
    sys.exit(app.exec_())

